# This is all about React Components.
- Components is a piece of UI that has its own Logic and appearance.
- They are reusable
- It is a JS Function that returns some HTML describing what should appear on screen.